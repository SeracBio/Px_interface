# Heavy / generated / private data — never committed (chemistry data stays on this machine)
data/
output/
ml/
interfaces/
autoresearch/
__MACOSX/
*.parquet
*.iface
srb_png/
volcanoes_px/
tmp/

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
px/

# Local editor / OS junk
.DS_Store
.vscode/
.idea/

# Claude Code per-machine settings
.claude/settings.local.json

# Internal AWS architecture diagram with real internal IPs/CIDRs — keep local only.
# The redacted docs/aws_architecture_mermaid.shared.md is the version that lives in git.
docs/aws_architecture_mermaid.md
